// Code generated by NodX codegen (attributes). DO NOT EDIT.
//
// Source data: https://github.com/varavelio/nodx/blob/main/data

import { Attr, AttrBool, AttrList } from "../attribute.ts";
import type { AttributeNode } from "../attribute.ts";
import type { AttributeValue, Node } from "../node.ts";

/**
 * Specifies an abbreviated version of the content in a header cell.
 *
 * @example Usage
 * ```ts
 * import { AbbrAttr } from "@varavel/nodx";
 *
 * const node = AbbrAttr("value");
 * // Renders as: abbr="value"
 * ```
 */
export function AbbrAttr(value: AttributeValue): AttributeNode {
  return Attr("abbr", value);
}

/**
 * Specifies the types of files that the server accepts (only for input type='file').
 *
 * @example Usage
 * ```ts
 * import { Accept } from "@varavel/nodx";
 *
 * const node = Accept("value");
 * // Renders as: accept="value"
 * ```
 */
export function Accept(value: AttributeValue): AttributeNode {
  return Attr("accept", value);
}

/**
 * Specifies the character encodings to be used for form submission.
 *
 * @example Usage
 * ```ts
 * import { AcceptCharset } from "@varavel/nodx";
 *
 * const node = AcceptCharset("value");
 * // Renders as: accept-charset="value"
 * ```
 */
export function AcceptCharset(value: AttributeValue): AttributeNode {
  return Attr("accept-charset", value);
}

/**
 * Specifies a shortcut key to activate/focus an element.
 *
 * @example Usage
 * ```ts
 * import { Accesskey } from "@varavel/nodx";
 *
 * const node = Accesskey("value-a value-b", "value-c");
 * // Renders as: accesskey="value-a value-b value-c"
 * ```
 */
export function Accesskey(
  ...values: readonly AttributeValue[]
): AttributeNode {
  return AttrList("accesskey", ...values);
}

/**
 * Specifies where to send the form-data when a form is submitted.
 *
 * @example Usage
 * ```ts
 * import { Action } from "@varavel/nodx";
 *
 * const node = Action("value");
 * // Renders as: action="value"
 * ```
 */
export function Action(value: AttributeValue): AttributeNode {
  return Attr("action", value);
}

/**
 * Specifies the alignment of the element's content.
 *
 * @example Usage
 * ```ts
 * import { Align } from "@varavel/nodx";
 *
 * const node = Align("value");
 * // Renders as: align="value"
 * ```
 */
export function Align(value: AttributeValue): AttributeNode {
  return Attr("align", value);
}

/**
 * Specifies a feature policy for the iframe.
 *
 * @example Usage
 * ```ts
 * import { Allow } from "@varavel/nodx";
 *
 * const node = Allow("value");
 * // Renders as: allow="value"
 * ```
 */
export function Allow(value: AttributeValue): AttributeNode {
  return Attr("allow", value);
}

/**
 * Specifies that an iframe can be displayed in fullscreen mode.
 *
 * @example Usage
 * ```ts
 * import { Allowfullscreen, Input } from "@varavel/nodx";
 *
 * const node = Input(Allowfullscreen(true));
 * // Renders as: <input allowfullscreen>
 *
 * // When the value is `false`, the attribute is omitted entirely:
 * const omitted = Input(Allowfullscreen(false));
 * // Renders as: <input>
 * ```
 */
export function Allowfullscreen(value: boolean): Node {
  return AttrBool("allowfullscreen", value);
}

/**
 * Provides alternative text for an image if it cannot be displayed.
 *
 * @example Usage
 * ```ts
 * import { Alt } from "@varavel/nodx";
 *
 * const node = Alt("value");
 * // Renders as: alt="value"
 * ```
 */
export function Alt(value: AttributeValue): AttributeNode {
  return Attr("alt", value);
}

/**
 * Specifies a space-separated list of archives for an object (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Archive } from "@varavel/nodx";
 *
 * const node = Archive("value");
 * // Renders as: archive="value"
 * ```
 */
export function Archive(value: AttributeValue): AttributeNode {
  return Attr("archive", value);
}

/**
 * Defines accessibility semantics or ARIA properties.
 *
 * @example Usage
 * ```ts
 * import { Aria } from "@varavel/nodx";
 *
 * const node = Aria("key", "value");
 * // Renders as: aria-key="value"
 * ```
 */
export function Aria(
  key: string,
  value: AttributeValue,
): AttributeNode {
  return Attr(`aria-${key}`, value);
}

/**
 * Specifies the type of content being preloaded (for link rel='preload').
 *
 * @example Usage
 * ```ts
 * import { As } from "@varavel/nodx";
 *
 * const node = As("value");
 * // Renders as: as="value"
 * ```
 */
export function As(value: AttributeValue): AttributeNode {
  return Attr("as", value);
}

/**
 * Indicates that the script should be executed asynchronously.
 *
 * @example Usage
 * ```ts
 * import { Async, Input } from "@varavel/nodx";
 *
 * const node = Input(Async(true));
 * // Renders as: <input async>
 *
 * // When the value is `false`, the attribute is omitted entirely:
 * const omitted = Input(Async(false));
 * // Renders as: <input>
 * ```
 */
export function Async(value: boolean): Node {
  return AttrBool("async", value);
}

/**
 * Controls how text input is automatically capitalized.
 *
 * @example Usage
 * ```ts
 * import { Autocapitalize } from "@varavel/nodx";
 *
 * const node = Autocapitalize("value");
 * // Renders as: autocapitalize="value"
 * ```
 */
export function Autocapitalize(value: AttributeValue): AttributeNode {
  return Attr("autocapitalize", value);
}

/**
 * Specifies whether an input field should have autocomplete enabled.
 *
 * @example Usage
 * ```ts
 * import { Autocomplete } from "@varavel/nodx";
 *
 * const node = Autocomplete("value-a value-b", "value-c");
 * // Renders as: autocomplete="value-a value-b value-c"
 * ```
 */
export function Autocomplete(
  ...values: readonly AttributeValue[]
): AttributeNode {
  return AttrList("autocomplete", ...values);
}

/**
 * Controls whether automatic spelling correction is enabled for a form field.
 *
 * @example Usage
 * ```ts
 * import { Autocorrect } from "@varavel/nodx";
 *
 * const node = Autocorrect("value");
 * // Renders as: autocorrect="value"
 * ```
 */
export function Autocorrect(value: AttributeValue): AttributeNode {
  return Attr("autocorrect", value);
}

/**
 * Specifies that an input field should automatically get focus when the page loads.
 *
 * @example Usage
 * ```ts
 * import { Autofocus, Input } from "@varavel/nodx";
 *
 * const node = Input(Autofocus(true));
 * // Renders as: <input autofocus>
 *
 * // When the value is `false`, the attribute is omitted entirely:
 * const omitted = Input(Autofocus(false));
 * // Renders as: <input>
 * ```
 */
export function Autofocus(value: boolean): Node {
  return AttrBool("autofocus", value);
}

/**
 * Specifies that the audio/video should start playing as soon as it is ready.
 *
 * @example Usage
 * ```ts
 * import { Autoplay, Input } from "@varavel/nodx";
 *
 * const node = Input(Autoplay(true));
 * // Renders as: <input autoplay>
 *
 * // When the value is `false`, the attribute is omitted entirely:
 * const omitted = Input(Autoplay(false));
 * // Renders as: <input>
 * ```
 */
export function Autoplay(value: boolean): Node {
  return AttrBool("autoplay", value);
}

/**
 * Specifies that an input should be pre-filled with previous values (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Autosave } from "@varavel/nodx";
 *
 * const node = Autosave("value");
 * // Renders as: autosave="value"
 * ```
 */
export function Autosave(value: AttributeValue): AttributeNode {
  return Attr("autosave", value);
}

/**
 * Specifies a category name for a table cell (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Axis } from "@varavel/nodx";
 *
 * const node = Axis("value");
 * // Renders as: axis="value"
 * ```
 */
export function Axis(value: AttributeValue): AttributeNode {
  return Attr("axis", value);
}

/**
 * Specifies a background image for the element.
 *
 * @example Usage
 * ```ts
 * import { Background } from "@varavel/nodx";
 *
 * const node = Background("value");
 * // Renders as: background="value"
 * ```
 */
export function Background(value: AttributeValue): AttributeNode {
  return Attr("background", value);
}

/**
 * Specifies the background color of an element.
 *
 * @example Usage
 * ```ts
 * import { Bgcolor } from "@varavel/nodx";
 *
 * const node = Bgcolor("value");
 * // Renders as: bgcolor="value"
 * ```
 */
export function Bgcolor(value: AttributeValue): AttributeNode {
  return Attr("bgcolor", value);
}

/**
 * Specifies that a script or stylesheet blocks rendering while loading.
 *
 * @example Usage
 * ```ts
 * import { Blocking } from "@varavel/nodx";
 *
 * const node = Blocking("value");
 * // Renders as: blocking="value"
 * ```
 */
export function Blocking(value: AttributeValue): AttributeNode {
  return Attr("blocking", value);
}

/**
 * Specifies the border width.
 *
 * @example Usage
 * ```ts
 * import { Border } from "@varavel/nodx";
 *
 * const node = Border("value");
 * // Renders as: border="value"
 * ```
 */
export function Border(value: AttributeValue): AttributeNode {
  return Attr("border", value);
}

/**
 * Contains the time ranges that the media element has buffered.
 *
 * @example Usage
 * ```ts
 * import { Buffered } from "@varavel/nodx";
 *
 * const node = Buffered("value");
 * // Renders as: buffered="value"
 * ```
 */
export function Buffered(value: AttributeValue): AttributeNode {
  return Attr("buffered", value);
}

/**
 * Specifies that the camera should be used for input.
 *
 * @example Usage
 * ```ts
 * import { Capture } from "@varavel/nodx";
 *
 * const node = Capture("value");
 * // Renders as: capture="value"
 * ```
 */
export function Capture(value: AttributeValue): AttributeNode {
  return Attr("capture", value);
}

/**
 * Specifies the space between cell walls and cell content (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Cellpadding } from "@varavel/nodx";
 *
 * const node = Cellpadding("value");
 * // Renders as: cellpadding="value"
 * ```
 */
export function Cellpadding(value: AttributeValue): AttributeNode {
  return Attr("cellpadding", value);
}

/**
 * Specifies the space between cells (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Cellspacing } from "@varavel/nodx";
 *
 * const node = Cellspacing("value");
 * // Renders as: cellspacing="value"
 * ```
 */
export function Cellspacing(value: AttributeValue): AttributeNode {
  return Attr("cellspacing", value);
}

/**
 * Specifies that the value of the keygen element should be challenged when submitted.
 *
 * @example Usage
 * ```ts
 * import { Challenge } from "@varavel/nodx";
 *
 * const node = Challenge("value");
 * // Renders as: challenge="value"
 * ```
 */
export function Challenge(value: AttributeValue): AttributeNode {
  return Attr("challenge", value);
}

/**
 * Aligns table content relative to a character (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Char } from "@varavel/nodx";
 *
 * const node = Char("value");
 * // Renders as: char="value"
 * ```
 */
export function Char(value: AttributeValue): AttributeNode {
  return Attr("char", value);
}

/**
 * Specifies the offset for the char attribute (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Charoff } from "@varavel/nodx";
 *
 * const node = Charoff("value");
 * // Renders as: charoff="value"
 * ```
 */
export function Charoff(value: AttributeValue): AttributeNode {
  return Attr("charoff", value);
}

/**
 * Specifies the character encoding.
 *
 * @example Usage
 * ```ts
 * import { Charset } from "@varavel/nodx";
 *
 * const node = Charset("value");
 * // Renders as: charset="value"
 * ```
 */
export function Charset(value: AttributeValue): AttributeNode {
  return Attr("charset", value);
}

/**
 * Specifies that an input element should be pre-selected when the page loads.
 *
 * @example Usage
 * ```ts
 * import { Checked, Input } from "@varavel/nodx";
 *
 * const node = Input(Checked(true));
 * // Renders as: <input checked>
 *
 * // When the value is `false`, the attribute is omitted entirely:
 * const omitted = Input(Checked(false));
 * // Renders as: <input>
 * ```
 */
export function Checked(value: boolean): Node {
  return AttrBool("checked", value);
}

/**
 * Specifies a URL that explains the quote, or why a text was inserted/deleted.
 *
 * @example Usage
 * ```ts
 * import { CiteAttr } from "@varavel/nodx";
 *
 * const node = CiteAttr("value");
 * // Renders as: cite="value"
 * ```
 */
export function CiteAttr(value: AttributeValue): AttributeNode {
  return Attr("cite", value);
}

/**
 * Specifies one or more class names for an element.
 *
 * @example Usage
 * ```ts
 * import { Class } from "@varavel/nodx";
 *
 * const node = Class("value-a value-b", "value-c");
 * // Renders as: class="value-a value-b value-c"
 * ```
 */
export function Class(
  ...values: readonly AttributeValue[]
): AttributeNode {
  return AttrList("class", ...values);
}

/**
 * Specifies the class id of an object (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Classid } from "@varavel/nodx";
 *
 * const node = Classid("value");
 * // Renders as: classid="value"
 * ```
 */
export function Classid(value: AttributeValue): AttributeNode {
  return Attr("classid", value);
}

/**
 * Specifies where the next line should appear after a line break (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Clear } from "@varavel/nodx";
 *
 * const node = Clear("value");
 * // Renders as: clear="value"
 * ```
 */
export function Clear(value: AttributeValue): AttributeNode {
  return Attr("clear", value);
}

/**
 * Specifies the URL of the applet's class file to be loaded and executed.
 *
 * @example Usage
 * ```ts
 * import { CodeAttr } from "@varavel/nodx";
 *
 * const node = CodeAttr("value");
 * // Renders as: code="value"
 * ```
 */
export function CodeAttr(value: AttributeValue): AttributeNode {
  return Attr("code", value);
}

/**
 * Specifies the base URL for an applet.
 *
 * @example Usage
 * ```ts
 * import { Codebase } from "@varavel/nodx";
 *
 * const node = Codebase("value");
 * // Renders as: codebase="value"
 * ```
 */
export function Codebase(value: AttributeValue): AttributeNode {
  return Attr("codebase", value);
}

/**
 * Specifies the MIME type of the code of an object (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Codetype } from "@varavel/nodx";
 *
 * const node = Codetype("value");
 * // Renders as: codetype="value"
 * ```
 */
export function Codetype(value: AttributeValue): AttributeNode {
  return Attr("codetype", value);
}

/**
 * Specifies the text color of an element.
 *
 * @example Usage
 * ```ts
 * import { Color } from "@varavel/nodx";
 *
 * const node = Color("value");
 * // Renders as: color="value"
 * ```
 */
export function Color(value: AttributeValue): AttributeNode {
  return Attr("color", value);
}

/**
 * Specifies the visible width of a text area.
 *
 * @example Usage
 * ```ts
 * import { Cols } from "@varavel/nodx";
 *
 * const node = Cols("value");
 * // Renders as: cols="value"
 * ```
 */
export function Cols(value: AttributeValue): AttributeNode {
  return Attr("cols", value);
}

/**
 * Specifies the number of columns a cell should span.
 *
 * @example Usage
 * ```ts
 * import { Colspan } from "@varavel/nodx";
 *
 * const node = Colspan("value");
 * // Renders as: colspan="value"
 * ```
 */
export function Colspan(value: AttributeValue): AttributeNode {
  return Attr("colspan", value);
}

/**
 * Specifies that a list should be rendered more compactly (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Compact } from "@varavel/nodx";
 *
 * const node = Compact("value");
 * // Renders as: compact="value"
 * ```
 */
export function Compact(value: AttributeValue): AttributeNode {
  return Attr("compact", value);
}

/**
 * Gives the value associated with the http-equiv or name attribute.
 *
 * @example Usage
 * ```ts
 * import { ContentAttr } from "@varavel/nodx";
 *
 * const node = ContentAttr("value");
 * // Renders as: content="value"
 * ```
 */
export function ContentAttr(value: AttributeValue): AttributeNode {
  return Attr("content", value);
}

/**
 * Specifies whether the content of an element is editable.
 *
 * @example Usage
 * ```ts
 * import { Contenteditable } from "@varavel/nodx";
 *
 * const node = Contenteditable("value");
 * // Renders as: contenteditable="value"
 * ```
 */
export function Contenteditable(value: AttributeValue): AttributeNode {
  return Attr("contenteditable", value);
}

/**
 * Specifies a context menu for an element.
 *
 * @example Usage
 * ```ts
 * import { Contextmenu } from "@varavel/nodx";
 *
 * const node = Contextmenu("value");
 * // Renders as: contextmenu="value"
 * ```
 */
export function Contextmenu(value: AttributeValue): AttributeNode {
  return Attr("contextmenu", value);
}

/**
 * Specifies that audio/video controls should be displayed.
 *
 * @example Usage
 * ```ts
 * import { Controls, Input } from "@varavel/nodx";
 *
 * const node = Input(Controls(true));
 * // Renders as: <input controls>
 *
 * // When the value is `false`, the attribute is omitted entirely:
 * const omitted = Input(Controls(false));
 * // Renders as: <input>
 * ```
 */
export function Controls(value: boolean): Node {
  return AttrBool("controls", value);
}

/**
 * Specifies the coordinates of an area in an image-map.
 *
 * @example Usage
 * ```ts
 * import { Coords } from "@varavel/nodx";
 *
 * const node = Coords("value");
 * // Renders as: coords="value"
 * ```
 */
export function Coords(value: AttributeValue): AttributeNode {
  return Attr("coords", value);
}

/**
 * Configures the CORS requests for the element's fetched data.
 *
 * @example Usage
 * ```ts
 * import { Crossorigin } from "@varavel/nodx";
 *
 * const node = Crossorigin("value");
 * // Renders as: crossorigin="value"
 * ```
 */
export function Crossorigin(value: AttributeValue): AttributeNode {
  return Attr("crossorigin", value);
}

/**
 * Allows specifying a Content Security Policy for the content in the iframe.
 *
 * @example Usage
 * ```ts
 * import { Csp } from "@varavel/nodx";
 *
 * const node = Csp("value");
 * // Renders as: csp="value"
 * ```
 */
export function Csp(value: AttributeValue): AttributeNode {
  return Attr("csp", value);
}

/**
 * Specifies the URL of the resource to be used by the object.
 *
 * @example Usage
 * ```ts
 * import { DataAttr } from "@varavel/nodx";
 *
 * const node = DataAttr("value");
 * // Renders as: data="value"
 * ```
 */
export function DataAttr(value: AttributeValue): AttributeNode {
  return Attr("data", value);
}

/**
 * Used to store custom data private to the page or application.
 *
 * @example Usage
 * ```ts
 * import { Data } from "@varavel/nodx";
 *
 * const node = Data("key", "value");
 * // Renders as: data-key="value"
 * ```
 */
export function Data(
  key: string,
  value: AttributeValue,
): AttributeNode {
  return Attr(`data-${key}`, value);
}

/**
 * Specifies the date and time.
 *
 * @example Usage
 * ```ts
 * import { Datetime } from "@varavel/nodx";
 *
 * const node = Datetime("value");
 * // Renders as: datetime="value"
 * ```
 */
export function Datetime(value: AttributeValue): AttributeNode {
  return Attr("datetime", value);
}

/**
 * Declares an object without instantiating it (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Declare } from "@varavel/nodx";
 *
 * const node = Declare("value");
 * // Renders as: declare="value"
 * ```
 */
export function Declare(value: AttributeValue): AttributeNode {
  return Attr("declare", value);
}

/**
 * Indicates how the browser should load the image.
 *
 * @example Usage
 * ```ts
 * import { Decoding } from "@varavel/nodx";
 *
 * const node = Decoding("value");
 * // Renders as: decoding="value"
 * ```
 */
export function Decoding(value: AttributeValue): AttributeNode {
  return Attr("decoding", value);
}

/**
 * Specifies the default track kind.
 *
 * @example Usage
 * ```ts
 * import { Default, Input } from "@varavel/nodx";
 *
 * const node = Input(Default(true));
 * // Renders as: <input default>
 *
 * // When the value is `false`, the attribute is omitted entirely:
 * const omitted = Input(Default(false));
 * // Renders as: <input>
 * ```
 */
export function Default(value: boolean): Node {
  return AttrBool("default", value);
}

/**
 * Specifies that the script is executed when the page has finished parsing.
 *
 * @example Usage
 * ```ts
 * import { Defer, Input } from "@varavel/nodx";
 *
 * const node = Input(Defer(true));
 * // Renders as: <input defer>
 *
 * // When the value is `false`, the attribute is omitted entirely:
 * const omitted = Input(Defer(false));
 * // Renders as: <input>
 * ```
 */
export function Defer(value: boolean): Node {
  return AttrBool("defer", value);
}

/**
 * Specifies the text direction for the content.
 *
 * @example Usage
 * ```ts
 * import { DirAttr } from "@varavel/nodx";
 *
 * const node = DirAttr("value");
 * // Renders as: dir="value"
 * ```
 */
export function DirAttr(value: AttributeValue): AttributeNode {
  return Attr("dir", value);
}

/**
 * Enables the submission of the text directionality of an input field.
 *
 * @example Usage
 * ```ts
 * import { Dirname } from "@varavel/nodx";
 *
 * const node = Dirname("value");
 * // Renders as: dirname="value"
 * ```
 */
export function Dirname(value: AttributeValue): AttributeNode {
  return Attr("dirname", value);
}

/**
 * Specifies that an element should be disabled.
 *
 * @example Usage
 * ```ts
 * import { Disabled, Input } from "@varavel/nodx";
 *
 * const node = Input(Disabled(true));
 * // Renders as: <input disabled>
 *
 * // When the value is `false`, the attribute is omitted entirely:
 * const omitted = Input(Disabled(false));
 * // Renders as: <input>
 * ```
 */
export function Disabled(value: boolean): Node {
  return AttrBool("disabled", value);
}

/**
 * Specifies that the target will be downloaded when a user clicks on the hyperlink.
 *
 * @example Usage
 * ```ts
 * import { Download } from "@varavel/nodx";
 *
 * const node = Download("value");
 * // Renders as: download="value"
 * ```
 */
export function Download(value: AttributeValue): AttributeNode {
  return Attr("download", value);
}

/**
 * Specifies whether an element is draggable or not.
 *
 * @example Usage
 * ```ts
 * import { Draggable } from "@varavel/nodx";
 *
 * const node = Draggable("value");
 * // Renders as: draggable="value"
 * ```
 */
export function Draggable(value: AttributeValue): AttributeNode {
  return Attr("draggable", value);
}

/**
 * Specifies how the form-data should be encoded when submitting it to the server.
 *
 * @example Usage
 * ```ts
 * import { Enctype } from "@varavel/nodx";
 *
 * const node = Enctype("value");
 * // Renders as: enctype="value"
 * ```
 */
export function Enctype(value: AttributeValue): AttributeNode {
  return Attr("enctype", value);
}

/**
 * Specifies what action label to present for the enter key on virtual keyboards.
 *
 * @example Usage
 * ```ts
 * import { Enterkeyhint } from "@varavel/nodx";
 *
 * const node = Enterkeyhint("value");
 * // Renders as: enterkeyhint="value"
 * ```
 */
export function Enterkeyhint(value: AttributeValue): AttributeNode {
  return Attr("enterkeyhint", value);
}

/**
 * Specifies the event a script is triggered by (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Event } from "@varavel/nodx";
 *
 * const node = Event("value");
 * // Renders as: event="value"
 * ```
 */
export function Event(value: AttributeValue): AttributeNode {
  return Attr("event", value);
}

/**
 * Exports shadow DOM parts of an element to its host page.
 *
 * @example Usage
 * ```ts
 * import { Exportparts } from "@varavel/nodx";
 *
 * const node = Exportparts("value");
 * // Renders as: exportparts="value"
 * ```
 */
export function Exportparts(value: AttributeValue): AttributeNode {
  return Attr("exportparts", value);
}

/**
 * Specifies the font family of the text (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Face } from "@varavel/nodx";
 *
 * const node = Face("value");
 * // Renders as: face="value"
 * ```
 */
export function Face(value: AttributeValue): AttributeNode {
  return Attr("face", value);
}

/**
 * Hints the priority for fetching a resource.
 *
 * @example Usage
 * ```ts
 * import { Fetchpriority } from "@varavel/nodx";
 *
 * const node = Fetchpriority("value");
 * // Renders as: fetchpriority="value"
 * ```
 */
export function Fetchpriority(value: AttributeValue): AttributeNode {
  return Attr("fetchpriority", value);
}

/**
 * Specifies which form element a label or output element is bound to.
 *
 * @example Usage
 * ```ts
 * import { For } from "@varavel/nodx";
 *
 * const node = For("value");
 * // Renders as: for="value"
 * ```
 */
export function For(value: AttributeValue): AttributeNode {
  return Attr("for", value);
}

/**
 * Specifies the form the element belongs to.
 *
 * @example Usage
 * ```ts
 * import { FormAttr } from "@varavel/nodx";
 *
 * const node = FormAttr("value");
 * // Renders as: form="value"
 * ```
 */
export function FormAttr(value: AttributeValue): AttributeNode {
  return Attr("form", value);
}

/**
 * Specifies where to send the form-data when a form is submitted (for input and button elements).
 *
 * @example Usage
 * ```ts
 * import { Formaction } from "@varavel/nodx";
 *
 * const node = Formaction("value");
 * // Renders as: formaction="value"
 * ```
 */
export function Formaction(value: AttributeValue): AttributeNode {
  return Attr("formaction", value);
}

/**
 * Specifies how form-data should be encoded (for input and button elements).
 *
 * @example Usage
 * ```ts
 * import { Formenctype } from "@varavel/nodx";
 *
 * const node = Formenctype("value");
 * // Renders as: formenctype="value"
 * ```
 */
export function Formenctype(value: AttributeValue): AttributeNode {
  return Attr("formenctype", value);
}

/**
 * Defines the HTTP method for sending form-data (for input and button elements).
 *
 * @example Usage
 * ```ts
 * import { Formmethod } from "@varavel/nodx";
 *
 * const node = Formmethod("value");
 * // Renders as: formmethod="value"
 * ```
 */
export function Formmethod(value: AttributeValue): AttributeNode {
  return Attr("formmethod", value);
}

/**
 * Defines that form elements should not be validated when submitted.
 *
 * @example Usage
 * ```ts
 * import { Formnovalidate, Input } from "@varavel/nodx";
 *
 * const node = Input(Formnovalidate(true));
 * // Renders as: <input formnovalidate>
 *
 * // When the value is `false`, the attribute is omitted entirely:
 * const omitted = Input(Formnovalidate(false));
 * // Renders as: <input>
 * ```
 */
export function Formnovalidate(value: boolean): Node {
  return AttrBool("formnovalidate", value);
}

/**
 * Specifies where to display the response after submitting the form.
 *
 * @example Usage
 * ```ts
 * import { Formtarget } from "@varavel/nodx";
 *
 * const node = Formtarget("value");
 * // Renders as: formtarget="value"
 * ```
 */
export function Formtarget(value: AttributeValue): AttributeNode {
  return Attr("formtarget", value);
}

/**
 * Specifies which parts of the table border should be visible (deprecated).
 *
 * @example Usage
 * ```ts
 * import { FrameAttr } from "@varavel/nodx";
 *
 * const node = FrameAttr("value");
 * // Renders as: frame="value"
 * ```
 */
export function FrameAttr(value: AttributeValue): AttributeNode {
  return Attr("frame", value);
}

/**
 * Specifies whether to display a border around an iframe (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Frameborder } from "@varavel/nodx";
 *
 * const node = Frameborder("value");
 * // Renders as: frameborder="value"
 * ```
 */
export function Frameborder(value: AttributeValue): AttributeNode {
  return Attr("frameborder", value);
}

/**
 * Specifies one or more header cells a cell is related to.
 *
 * @example Usage
 * ```ts
 * import { Headers } from "@varavel/nodx";
 *
 * const node = Headers("value-a value-b", "value-c");
 * // Renders as: headers="value-a value-b value-c"
 * ```
 */
export function Headers(
  ...values: readonly AttributeValue[]
): AttributeNode {
  return AttrList("headers", ...values);
}

/**
 * Specifies the height of the element.
 *
 * @example Usage
 * ```ts
 * import { Height } from "@varavel/nodx";
 *
 * const node = Height("value");
 * // Renders as: height="value"
 * ```
 */
export function Height(value: AttributeValue): AttributeNode {
  return Attr("height", value);
}

/**
 * Specifies that an element is not yet, or is no longer, relevant.
 *
 * @example Usage
 * ```ts
 * import { Hidden } from "@varavel/nodx";
 *
 * const node = Hidden("value");
 * // Renders as: hidden="value"
 * ```
 */
export function Hidden(value: AttributeValue): AttributeNode {
  return Attr("hidden", value);
}

/**
 * Specifies the range that is considered to be a high value.
 *
 * @example Usage
 * ```ts
 * import { High } from "@varavel/nodx";
 *
 * const node = High("value");
 * // Renders as: high="value"
 * ```
 */
export function High(value: AttributeValue): AttributeNode {
  return Attr("high", value);
}

/**
 * Specifies the URL of the page the link goes to.
 *
 * @example Usage
 * ```ts
 * import { Href } from "@varavel/nodx";
 *
 * const node = Href("value");
 * // Renders as: href="value"
 * ```
 */
export function Href(value: AttributeValue): AttributeNode {
  return Attr("href", value);
}

/**
 * Specifies the language of the linked document.
 *
 * @example Usage
 * ```ts
 * import { Hreflang } from "@varavel/nodx";
 *
 * const node = Hreflang("value");
 * // Renders as: hreflang="value"
 * ```
 */
export function Hreflang(value: AttributeValue): AttributeNode {
  return Attr("hreflang", value);
}

/**
 * Specifies the horizontal space around an element (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Hspace } from "@varavel/nodx";
 *
 * const node = Hspace("value");
 * // Renders as: hspace="value"
 * ```
 */
export function Hspace(value: AttributeValue): AttributeNode {
  return Attr("hspace", value);
}

/**
 * Provides an HTTP header for the information/value of the content attribute.
 *
 * @example Usage
 * ```ts
 * import { HttpEquiv } from "@varavel/nodx";
 *
 * const node = HttpEquiv("value");
 * // Renders as: http-equiv="value"
 * ```
 */
export function HttpEquiv(value: AttributeValue): AttributeNode {
  return Attr("http-equiv", value);
}

/**
 * Specifies an icon for the command.
 *
 * @example Usage
 * ```ts
 * import { Icon } from "@varavel/nodx";
 *
 * const node = Icon("value");
 * // Renders as: icon="value"
 * ```
 */
export function Icon(value: AttributeValue): AttributeNode {
  return Attr("icon", value);
}

/**
 * Specifies a unique id for an element.
 *
 * @example Usage
 * ```ts
 * import { Id } from "@varavel/nodx";
 *
 * const node = Id("value");
 * // Renders as: id="value"
 * ```
 */
export function Id(value: AttributeValue): AttributeNode {
  return Attr("id", value);
}

/**
 * Specifies image sizes for preloaded images (link rel='preload').
 *
 * @example Usage
 * ```ts
 * import { Imagesizes } from "@varavel/nodx";
 *
 * const node = Imagesizes("value");
 * // Renders as: imagesizes="value"
 * ```
 */
export function Imagesizes(value: AttributeValue): AttributeNode {
  return Attr("imagesizes", value);
}

/**
 * Specifies image candidates for preloaded images (link rel='preload').
 *
 * @example Usage
 * ```ts
 * import { Imagesrcset } from "@varavel/nodx";
 *
 * const node = Imagesrcset("value");
 * // Renders as: imagesrcset="value"
 * ```
 */
export function Imagesrcset(value: AttributeValue): AttributeNode {
  return Attr("imagesrcset", value);
}

/**
 * Indicates the relative fetch priority for the resource.
 *
 * @example Usage
 * ```ts
 * import { Importance } from "@varavel/nodx";
 *
 * const node = Importance("value");
 * // Renders as: importance="value"
 * ```
 */
export function Importance(value: AttributeValue): AttributeNode {
  return Attr("importance", value);
}

/**
 * Makes an element and its children unresponsive to user input.
 *
 * @example Usage
 * ```ts
 * import { Inert, Input } from "@varavel/nodx";
 *
 * const node = Input(Inert(true));
 * // Renders as: <input inert>
 *
 * // When the value is `false`, the attribute is omitted entirely:
 * const omitted = Input(Inert(false));
 * // Renders as: <input>
 * ```
 */
export function Inert(value: boolean): Node {
  return AttrBool("inert", value);
}

/**
 * Hints the type of virtual keyboard to use for a form field.
 *
 * @example Usage
 * ```ts
 * import { Inputmode } from "@varavel/nodx";
 *
 * const node = Inputmode("value");
 * // Renders as: inputmode="value"
 * ```
 */
export function Inputmode(value: AttributeValue): AttributeNode {
  return Attr("inputmode", value);
}

/**
 * Allows a browser to verify the fetched resource's integrity.
 *
 * @example Usage
 * ```ts
 * import { Integrity } from "@varavel/nodx";
 *
 * const node = Integrity("value");
 * // Renders as: integrity="value"
 * ```
 */
export function Integrity(value: AttributeValue): AttributeNode {
  return Attr("integrity", value);
}

/**
 * Specifies an image as a server-side image-map.
 *
 * @example Usage
 * ```ts
 * import { Ismap, Input } from "@varavel/nodx";
 *
 * const node = Input(Ismap(true));
 * // Renders as: <input ismap>
 *
 * // When the value is `false`, the attribute is omitted entirely:
 * const omitted = Input(Ismap(false));
 * // Renders as: <input>
 * ```
 */
export function Ismap(value: boolean): Node {
  return AttrBool("ismap", value);
}

/**
 * Specifies a global identifier for a microdata item.
 *
 * @example Usage
 * ```ts
 * import { Itemid } from "@varavel/nodx";
 *
 * const node = Itemid("value");
 * // Renders as: itemid="value"
 * ```
 */
export function Itemid(value: AttributeValue): AttributeNode {
  return Attr("itemid", value);
}

/**
 * Defines a property of an item.
 *
 * @example Usage
 * ```ts
 * import { Itemprop } from "@varavel/nodx";
 *
 * const node = Itemprop("value-a value-b", "value-c");
 * // Renders as: itemprop="value-a value-b value-c"
 * ```
 */
export function Itemprop(
  ...values: readonly AttributeValue[]
): AttributeNode {
  return AttrList("itemprop", ...values);
}

/**
 * References additional elements that provide properties for a microdata item.
 *
 * @example Usage
 * ```ts
 * import { Itemref } from "@varavel/nodx";
 *
 * const node = Itemref("value");
 * // Renders as: itemref="value"
 * ```
 */
export function Itemref(value: AttributeValue): AttributeNode {
  return Attr("itemref", value);
}

/**
 * Creates a microdata item.
 *
 * @example Usage
 * ```ts
 * import { Itemscope, Input } from "@varavel/nodx";
 *
 * const node = Input(Itemscope(true));
 * // Renders as: <input itemscope>
 *
 * // When the value is `false`, the attribute is omitted entirely:
 * const omitted = Input(Itemscope(false));
 * // Renders as: <input>
 * ```
 */
export function Itemscope(value: boolean): Node {
  return AttrBool("itemscope", value);
}

/**
 * Specifies the vocabulary used by a microdata item.
 *
 * @example Usage
 * ```ts
 * import { Itemtype } from "@varavel/nodx";
 *
 * const node = Itemtype("value-a value-b", "value-c");
 * // Renders as: itemtype="value-a value-b value-c"
 * ```
 */
export function Itemtype(
  ...values: readonly AttributeValue[]
): AttributeNode {
  return AttrList("itemtype", ...values);
}

/**
 * Specifies the security algorithm of a key.
 *
 * @example Usage
 * ```ts
 * import { Keytype } from "@varavel/nodx";
 *
 * const node = Keytype("value");
 * // Renders as: keytype="value"
 * ```
 */
export function Keytype(value: AttributeValue): AttributeNode {
  return Attr("keytype", value);
}

/**
 * Specifies the kind of text track.
 *
 * @example Usage
 * ```ts
 * import { Kind } from "@varavel/nodx";
 *
 * const node = Kind("value");
 * // Renders as: kind="value"
 * ```
 */
export function Kind(value: AttributeValue): AttributeNode {
  return Attr("kind", value);
}

/**
 * Specifies the title of the text track.
 *
 * @example Usage
 * ```ts
 * import { LabelAttr } from "@varavel/nodx";
 *
 * const node = LabelAttr("value");
 * // Renders as: label="value"
 * ```
 */
export function LabelAttr(value: AttributeValue): AttributeNode {
  return Attr("label", value);
}

/**
 * Specifies the language of the element's content.
 *
 * @example Usage
 * ```ts
 * import { Lang } from "@varavel/nodx";
 *
 * const node = Lang("value");
 * // Renders as: lang="value"
 * ```
 */
export function Lang(value: AttributeValue): AttributeNode {
  return Attr("lang", value);
}

/**
 * Deprecated. Specifies the scripting language used for the script.
 *
 * @example Usage
 * ```ts
 * import { Language } from "@varavel/nodx";
 *
 * const node = Language("value");
 * // Renders as: language="value"
 * ```
 */
export function Language(value: AttributeValue): AttributeNode {
  return Attr("language", value);
}

/**
 * Refers to a datalist element that contains pre-defined options.
 *
 * @example Usage
 * ```ts
 * import { List } from "@varavel/nodx";
 *
 * const node = List("value");
 * // Renders as: list="value"
 * ```
 */
export function List(value: AttributeValue): AttributeNode {
  return Attr("list", value);
}

/**
 * Indicates how the browser should load the image or iframe.
 *
 * @example Usage
 * ```ts
 * import { Loading } from "@varavel/nodx";
 *
 * const node = Loading("value");
 * // Renders as: loading="value"
 * ```
 */
export function Loading(value: AttributeValue): AttributeNode {
  return Attr("loading", value);
}

/**
 * Specifies a URL with a detailed description of an image (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Longdesc } from "@varavel/nodx";
 *
 * const node = Longdesc("value");
 * // Renders as: longdesc="value"
 * ```
 */
export function Longdesc(value: AttributeValue): AttributeNode {
  return Attr("longdesc", value);
}

/**
 * Specifies that the audio/video will start over again, every time it is finished.
 *
 * @example Usage
 * ```ts
 * import { Loop, Input } from "@varavel/nodx";
 *
 * const node = Input(Loop(true));
 * // Renders as: <input loop>
 *
 * // When the value is `false`, the attribute is omitted entirely:
 * const omitted = Input(Loop(false));
 * // Renders as: <input>
 * ```
 */
export function Loop(value: boolean): Node {
  return AttrBool("loop", value);
}

/**
 * Specifies the range that is considered to be a low value.
 *
 * @example Usage
 * ```ts
 * import { Low } from "@varavel/nodx";
 *
 * const node = Low("value");
 * // Renders as: low="value"
 * ```
 */
export function Low(value: AttributeValue): AttributeNode {
  return Attr("low", value);
}

/**
 * Specifies the URL of the document's cache manifest.
 *
 * @example Usage
 * ```ts
 * import { Manifest } from "@varavel/nodx";
 *
 * const node = Manifest("value");
 * // Renders as: manifest="value"
 * ```
 */
export function Manifest(value: AttributeValue): AttributeNode {
  return Attr("manifest", value);
}

/**
 * Specifies the top and bottom margins of an iframe (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Marginheight } from "@varavel/nodx";
 *
 * const node = Marginheight("value");
 * // Renders as: marginheight="value"
 * ```
 */
export function Marginheight(value: AttributeValue): AttributeNode {
  return Attr("marginheight", value);
}

/**
 * Specifies the left and right margins of an iframe (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Marginwidth } from "@varavel/nodx";
 *
 * const node = Marginwidth("value");
 * // Renders as: marginwidth="value"
 * ```
 */
export function Marginwidth(value: AttributeValue): AttributeNode {
  return Attr("marginwidth", value);
}

/**
 * Specifies the maximum value.
 *
 * @example Usage
 * ```ts
 * import { Max } from "@varavel/nodx";
 *
 * const node = Max("value");
 * // Renders as: max="value"
 * ```
 */
export function Max(value: AttributeValue): AttributeNode {
  return Attr("max", value);
}

/**
 * Specifies the maximum number of characters allowed in an input field.
 *
 * @example Usage
 * ```ts
 * import { Maxlength } from "@varavel/nodx";
 *
 * const node = Maxlength("value");
 * // Renders as: maxlength="value"
 * ```
 */
export function Maxlength(value: AttributeValue): AttributeNode {
  return Attr("maxlength", value);
}

/**
 * Specifies what media/device the linked document is optimized for.
 *
 * @example Usage
 * ```ts
 * import { Media } from "@varavel/nodx";
 *
 * const node = Media("value");
 * // Renders as: media="value"
 * ```
 */
export function Media(value: AttributeValue): AttributeNode {
  return Attr("media", value);
}

/**
 * Specifies the HTTP method to use when sending form-data.
 *
 * @example Usage
 * ```ts
 * import { Method } from "@varavel/nodx";
 *
 * const node = Method("value");
 * // Renders as: method="value"
 * ```
 */
export function Method(value: AttributeValue): AttributeNode {
  return Attr("method", value);
}

/**
 * Specifies the minimum value.
 *
 * @example Usage
 * ```ts
 * import { Min } from "@varavel/nodx";
 *
 * const node = Min("value");
 * // Renders as: min="value"
 * ```
 */
export function Min(value: AttributeValue): AttributeNode {
  return Attr("min", value);
}

/**
 * Specifies the minimum number of characters required in an input field.
 *
 * @example Usage
 * ```ts
 * import { Minlength } from "@varavel/nodx";
 *
 * const node = Minlength("value");
 * // Renders as: minlength="value"
 * ```
 */
export function Minlength(value: AttributeValue): AttributeNode {
  return Attr("minlength", value);
}

/**
 * Specifies that a user can enter more than one value.
 *
 * @example Usage
 * ```ts
 * import { Multiple, Input } from "@varavel/nodx";
 *
 * const node = Input(Multiple(true));
 * // Renders as: <input multiple>
 *
 * // When the value is `false`, the attribute is omitted entirely:
 * const omitted = Input(Multiple(false));
 * // Renders as: <input>
 * ```
 */
export function Multiple(value: boolean): Node {
  return AttrBool("multiple", value);
}

/**
 * Specifies that the audio output should be muted.
 *
 * @example Usage
 * ```ts
 * import { Muted, Input } from "@varavel/nodx";
 *
 * const node = Input(Muted(true));
 * // Renders as: <input muted>
 *
 * // When the value is `false`, the attribute is omitted entirely:
 * const omitted = Input(Muted(false));
 * // Renders as: <input>
 * ```
 */
export function Muted(value: boolean): Node {
  return AttrBool("muted", value);
}

/**
 * Specifies the name of the element.
 *
 * @example Usage
 * ```ts
 * import { Name } from "@varavel/nodx";
 *
 * const node = Name("value");
 * // Renders as: name="value"
 * ```
 */
export function Name(value: AttributeValue): AttributeNode {
  return Attr("name", value);
}

/**
 * Specifies that an area has no associated link (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Nohref } from "@varavel/nodx";
 *
 * const node = Nohref("value");
 * // Renders as: nohref="value"
 * ```
 */
export function Nohref(value: AttributeValue): AttributeNode {
  return Attr("nohref", value);
}

/**
 * Indicates that the script should not be executed in browsers that support module scripts.
 *
 * @example Usage
 * ```ts
 * import { Nomodule, Input } from "@varavel/nodx";
 *
 * const node = Input(Nomodule(true));
 * // Renders as: <input nomodule>
 *
 * // When the value is `false`, the attribute is omitted entirely:
 * const omitted = Input(Nomodule(false));
 * // Renders as: <input>
 * ```
 */
export function Nomodule(value: boolean): Node {
  return AttrBool("nomodule", value);
}

/**
 * A cryptographic nonce used in Content Security Policy.
 *
 * @example Usage
 * ```ts
 * import { Nonce } from "@varavel/nodx";
 *
 * const node = Nonce("value");
 * // Renders as: nonce="value"
 * ```
 */
export function Nonce(value: AttributeValue): AttributeNode {
  return Attr("nonce", value);
}

/**
 * Specifies that a frame cannot be resized (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Noresize } from "@varavel/nodx";
 *
 * const node = Noresize("value");
 * // Renders as: noresize="value"
 * ```
 */
export function Noresize(value: AttributeValue): AttributeNode {
  return Attr("noresize", value);
}

/**
 * Specifies that a horizontal rule has no shading (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Noshade } from "@varavel/nodx";
 *
 * const node = Noshade("value");
 * // Renders as: noshade="value"
 * ```
 */
export function Noshade(value: AttributeValue): AttributeNode {
  return Attr("noshade", value);
}

/**
 * Specifies that the form should not be validated when submitted.
 *
 * @example Usage
 * ```ts
 * import { Novalidate, Input } from "@varavel/nodx";
 *
 * const node = Input(Novalidate(true));
 * // Renders as: <input novalidate>
 *
 * // When the value is `false`, the attribute is omitted entirely:
 * const omitted = Input(Novalidate(false));
 * // Renders as: <input>
 * ```
 */
export function Novalidate(value: boolean): Node {
  return AttrBool("novalidate", value);
}

/**
 * Specifies that content should not wrap (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Nowrap } from "@varavel/nodx";
 *
 * const node = Nowrap("value");
 * // Renders as: nowrap="value"
 * ```
 */
export function Nowrap(value: AttributeValue): AttributeNode {
  return Attr("nowrap", value);
}

/**
 * Fires when the loading of a resource is aborted.
 *
 * @example Usage
 * ```ts
 * import { Onabort } from "@varavel/nodx";
 *
 * const node = Onabort("value");
 * // Renders as: onabort="value"
 * ```
 */
export function Onabort(value: AttributeValue): AttributeNode {
  return Attr("onabort", value);
}

/**
 * Fires when a CSS animation is unexpectedly interrupted.
 *
 * @example Usage
 * ```ts
 * import { Onanimationcancel } from "@varavel/nodx";
 *
 * const node = Onanimationcancel("value");
 * // Renders as: onanimationcancel="value"
 * ```
 */
export function Onanimationcancel(value: AttributeValue): AttributeNode {
  return Attr("onanimationcancel", value);
}

/**
 * Fires when a CSS animation finishes.
 *
 * @example Usage
 * ```ts
 * import { Onanimationend } from "@varavel/nodx";
 *
 * const node = Onanimationend("value");
 * // Renders as: onanimationend="value"
 * ```
 */
export function Onanimationend(value: AttributeValue): AttributeNode {
  return Attr("onanimationend", value);
}

/**
 * Fires when a CSS animation repeats.
 *
 * @example Usage
 * ```ts
 * import { Onanimationiteration } from "@varavel/nodx";
 *
 * const node = Onanimationiteration("value");
 * // Renders as: onanimationiteration="value"
 * ```
 */
export function Onanimationiteration(value: AttributeValue): AttributeNode {
  return Attr("onanimationiteration", value);
}

/**
 * Fires when a CSS animation starts.
 *
 * @example Usage
 * ```ts
 * import { Onanimationstart } from "@varavel/nodx";
 *
 * const node = Onanimationstart("value");
 * // Renders as: onanimationstart="value"
 * ```
 */
export function Onanimationstart(value: AttributeValue): AttributeNode {
  return Attr("onanimationstart", value);
}

/**
 * Fires when a non-primary mouse button is clicked.
 *
 * @example Usage
 * ```ts
 * import { Onauxclick } from "@varavel/nodx";
 *
 * const node = Onauxclick("value");
 * // Renders as: onauxclick="value"
 * ```
 */
export function Onauxclick(value: AttributeValue): AttributeNode {
  return Attr("onauxclick", value);
}

/**
 * Fires when an input event is about to fire.
 *
 * @example Usage
 * ```ts
 * import { Onbeforeinput } from "@varavel/nodx";
 *
 * const node = Onbeforeinput("value");
 * // Renders as: onbeforeinput="value"
 * ```
 */
export function Onbeforeinput(value: AttributeValue): AttributeNode {
  return Attr("onbeforeinput", value);
}

/**
 * Fires when a hidden section is matched by the find-in-page feature.
 *
 * @example Usage
 * ```ts
 * import { Onbeforematch } from "@varavel/nodx";
 *
 * const node = Onbeforematch("value");
 * // Renders as: onbeforematch="value"
 * ```
 */
export function Onbeforematch(value: AttributeValue): AttributeNode {
  return Attr("onbeforematch", value);
}

/**
 * Fires when a popover or details element is about to toggle.
 *
 * @example Usage
 * ```ts
 * import { Onbeforetoggle } from "@varavel/nodx";
 *
 * const node = Onbeforetoggle("value");
 * // Renders as: onbeforetoggle="value"
 * ```
 */
export function Onbeforetoggle(value: AttributeValue): AttributeNode {
  return Attr("onbeforetoggle", value);
}

/**
 * Fires when an element loses focus.
 *
 * @example Usage
 * ```ts
 * import { Onblur } from "@varavel/nodx";
 *
 * const node = Onblur("value");
 * // Renders as: onblur="value"
 * ```
 */
export function Onblur(value: AttributeValue): AttributeNode {
  return Attr("onblur", value);
}

/**
 * Fires when the user cancels a dialog.
 *
 * @example Usage
 * ```ts
 * import { Oncancel } from "@varavel/nodx";
 *
 * const node = Oncancel("value");
 * // Renders as: oncancel="value"
 * ```
 */
export function Oncancel(value: AttributeValue): AttributeNode {
  return Attr("oncancel", value);
}

/**
 * Fires when the media can start playing.
 *
 * @example Usage
 * ```ts
 * import { Oncanplay } from "@varavel/nodx";
 *
 * const node = Oncanplay("value");
 * // Renders as: oncanplay="value"
 * ```
 */
export function Oncanplay(value: AttributeValue): AttributeNode {
  return Attr("oncanplay", value);
}

/**
 * Fires when the media can play through without buffering.
 *
 * @example Usage
 * ```ts
 * import { Oncanplaythrough } from "@varavel/nodx";
 *
 * const node = Oncanplaythrough("value");
 * // Renders as: oncanplaythrough="value"
 * ```
 */
export function Oncanplaythrough(value: AttributeValue): AttributeNode {
  return Attr("oncanplaythrough", value);
}

/**
 * Fires when the value of a form element changes.
 *
 * @example Usage
 * ```ts
 * import { Onchange } from "@varavel/nodx";
 *
 * const node = Onchange("value");
 * // Renders as: onchange="value"
 * ```
 */
export function Onchange(value: AttributeValue): AttributeNode {
  return Attr("onchange", value);
}

/**
 * Fires when the user clicks an element.
 *
 * @example Usage
 * ```ts
 * import { Onclick } from "@varavel/nodx";
 *
 * const node = Onclick("value");
 * // Renders as: onclick="value"
 * ```
 */
export function Onclick(value: AttributeValue): AttributeNode {
  return Attr("onclick", value);
}

/**
 * Fires when a dialog is closed.
 *
 * @example Usage
 * ```ts
 * import { Onclose } from "@varavel/nodx";
 *
 * const node = Onclose("value");
 * // Renders as: onclose="value"
 * ```
 */
export function Onclose(value: AttributeValue): AttributeNode {
  return Attr("onclose", value);
}

/**
 * Fires when a context menu cannot open.
 *
 * @example Usage
 * ```ts
 * import { Oncontextlost } from "@varavel/nodx";
 *
 * const node = Oncontextlost("value");
 * // Renders as: oncontextlost="value"
 * ```
 */
export function Oncontextlost(value: AttributeValue): AttributeNode {
  return Attr("oncontextlost", value);
}

/**
 * Fires when the user opens a context menu.
 *
 * @example Usage
 * ```ts
 * import { Oncontextmenu } from "@varavel/nodx";
 *
 * const node = Oncontextmenu("value");
 * // Renders as: oncontextmenu="value"
 * ```
 */
export function Oncontextmenu(value: AttributeValue): AttributeNode {
  return Attr("oncontextmenu", value);
}

/**
 * Fires when a context menu is restored.
 *
 * @example Usage
 * ```ts
 * import { Oncontextrestored } from "@varavel/nodx";
 *
 * const node = Oncontextrestored("value");
 * // Renders as: oncontextrestored="value"
 * ```
 */
export function Oncontextrestored(value: AttributeValue): AttributeNode {
  return Attr("oncontextrestored", value);
}

/**
 * Fires when the user copies content.
 *
 * @example Usage
 * ```ts
 * import { Oncopy } from "@varavel/nodx";
 *
 * const node = Oncopy("value");
 * // Renders as: oncopy="value"
 * ```
 */
export function Oncopy(value: AttributeValue): AttributeNode {
  return Attr("oncopy", value);
}

/**
 * Fires when the current cue of a text track changes.
 *
 * @example Usage
 * ```ts
 * import { Oncuechange } from "@varavel/nodx";
 *
 * const node = Oncuechange("value");
 * // Renders as: oncuechange="value"
 * ```
 */
export function Oncuechange(value: AttributeValue): AttributeNode {
  return Attr("oncuechange", value);
}

/**
 * Fires when the user cuts content.
 *
 * @example Usage
 * ```ts
 * import { Oncut } from "@varavel/nodx";
 *
 * const node = Oncut("value");
 * // Renders as: oncut="value"
 * ```
 */
export function Oncut(value: AttributeValue): AttributeNode {
  return Attr("oncut", value);
}

/**
 * Fires when the user double-clicks an element.
 *
 * @example Usage
 * ```ts
 * import { Ondblclick } from "@varavel/nodx";
 *
 * const node = Ondblclick("value");
 * // Renders as: ondblclick="value"
 * ```
 */
export function Ondblclick(value: AttributeValue): AttributeNode {
  return Attr("ondblclick", value);
}

/**
 * Fires when an element is being dragged.
 *
 * @example Usage
 * ```ts
 * import { Ondrag } from "@varavel/nodx";
 *
 * const node = Ondrag("value");
 * // Renders as: ondrag="value"
 * ```
 */
export function Ondrag(value: AttributeValue): AttributeNode {
  return Attr("ondrag", value);
}

/**
 * Fires when a drag operation ends.
 *
 * @example Usage
 * ```ts
 * import { Ondragend } from "@varavel/nodx";
 *
 * const node = Ondragend("value");
 * // Renders as: ondragend="value"
 * ```
 */
export function Ondragend(value: AttributeValue): AttributeNode {
  return Attr("ondragend", value);
}

/**
 * Fires when a dragged element enters a drop target.
 *
 * @example Usage
 * ```ts
 * import { Ondragenter } from "@varavel/nodx";
 *
 * const node = Ondragenter("value");
 * // Renders as: ondragenter="value"
 * ```
 */
export function Ondragenter(value: AttributeValue): AttributeNode {
  return Attr("ondragenter", value);
}

/**
 * Fires when a dragged element leaves a drop target.
 *
 * @example Usage
 * ```ts
 * import { Ondragleave } from "@varavel/nodx";
 *
 * const node = Ondragleave("value");
 * // Renders as: ondragleave="value"
 * ```
 */
export function Ondragleave(value: AttributeValue): AttributeNode {
  return Attr("ondragleave", value);
}

/**
 * Fires when a dragged element is over a drop target.
 *
 * @example Usage
 * ```ts
 * import { Ondragover } from "@varavel/nodx";
 *
 * const node = Ondragover("value");
 * // Renders as: ondragover="value"
 * ```
 */
export function Ondragover(value: AttributeValue): AttributeNode {
  return Attr("ondragover", value);
}

/**
 * Fires when a drag operation starts.
 *
 * @example Usage
 * ```ts
 * import { Ondragstart } from "@varavel/nodx";
 *
 * const node = Ondragstart("value");
 * // Renders as: ondragstart="value"
 * ```
 */
export function Ondragstart(value: AttributeValue): AttributeNode {
  return Attr("ondragstart", value);
}

/**
 * Fires when a dragged element is dropped.
 *
 * @example Usage
 * ```ts
 * import { Ondrop } from "@varavel/nodx";
 *
 * const node = Ondrop("value");
 * // Renders as: ondrop="value"
 * ```
 */
export function Ondrop(value: AttributeValue): AttributeNode {
  return Attr("ondrop", value);
}

/**
 * Fires when the duration of the media changes.
 *
 * @example Usage
 * ```ts
 * import { Ondurationchange } from "@varavel/nodx";
 *
 * const node = Ondurationchange("value");
 * // Renders as: ondurationchange="value"
 * ```
 */
export function Ondurationchange(value: AttributeValue): AttributeNode {
  return Attr("ondurationchange", value);
}

/**
 * Fires when the media becomes empty.
 *
 * @example Usage
 * ```ts
 * import { Onemptied } from "@varavel/nodx";
 *
 * const node = Onemptied("value");
 * // Renders as: onemptied="value"
 * ```
 */
export function Onemptied(value: AttributeValue): AttributeNode {
  return Attr("onemptied", value);
}

/**
 * Fires when the media reaches the end.
 *
 * @example Usage
 * ```ts
 * import { Onended } from "@varavel/nodx";
 *
 * const node = Onended("value");
 * // Renders as: onended="value"
 * ```
 */
export function Onended(value: AttributeValue): AttributeNode {
  return Attr("onended", value);
}

/**
 * Fires when a resource fails to load.
 *
 * @example Usage
 * ```ts
 * import { Onerror } from "@varavel/nodx";
 *
 * const node = Onerror("value");
 * // Renders as: onerror="value"
 * ```
 */
export function Onerror(value: AttributeValue): AttributeNode {
  return Attr("onerror", value);
}

/**
 * Fires when an element gains focus.
 *
 * @example Usage
 * ```ts
 * import { Onfocus } from "@varavel/nodx";
 *
 * const node = Onfocus("value");
 * // Renders as: onfocus="value"
 * ```
 */
export function Onfocus(value: AttributeValue): AttributeNode {
  return Attr("onfocus", value);
}

/**
 * Fires when the form data set is built.
 *
 * @example Usage
 * ```ts
 * import { Onformdata } from "@varavel/nodx";
 *
 * const node = Onformdata("value");
 * // Renders as: onformdata="value"
 * ```
 */
export function Onformdata(value: AttributeValue): AttributeNode {
  return Attr("onformdata", value);
}

/**
 * Fires when a pointer is captured.
 *
 * @example Usage
 * ```ts
 * import { Ongotpointercapture } from "@varavel/nodx";
 *
 * const node = Ongotpointercapture("value");
 * // Renders as: ongotpointercapture="value"
 * ```
 */
export function Ongotpointercapture(value: AttributeValue): AttributeNode {
  return Attr("ongotpointercapture", value);
}

/**
 * Fires when the value of an input element changes.
 *
 * @example Usage
 * ```ts
 * import { Oninput } from "@varavel/nodx";
 *
 * const node = Oninput("value");
 * // Renders as: oninput="value"
 * ```
 */
export function Oninput(value: AttributeValue): AttributeNode {
  return Attr("oninput", value);
}

/**
 * Fires when a form element fails validation.
 *
 * @example Usage
 * ```ts
 * import { Oninvalid } from "@varavel/nodx";
 *
 * const node = Oninvalid("value");
 * // Renders as: oninvalid="value"
 * ```
 */
export function Oninvalid(value: AttributeValue): AttributeNode {
  return Attr("oninvalid", value);
}

/**
 * Fires when a key is pressed down.
 *
 * @example Usage
 * ```ts
 * import { Onkeydown } from "@varavel/nodx";
 *
 * const node = Onkeydown("value");
 * // Renders as: onkeydown="value"
 * ```
 */
export function Onkeydown(value: AttributeValue): AttributeNode {
  return Attr("onkeydown", value);
}

/**
 * Fires when a key is pressed (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Onkeypress } from "@varavel/nodx";
 *
 * const node = Onkeypress("value");
 * // Renders as: onkeypress="value"
 * ```
 */
export function Onkeypress(value: AttributeValue): AttributeNode {
  return Attr("onkeypress", value);
}

/**
 * Fires when a key is released.
 *
 * @example Usage
 * ```ts
 * import { Onkeyup } from "@varavel/nodx";
 *
 * const node = Onkeyup("value");
 * // Renders as: onkeyup="value"
 * ```
 */
export function Onkeyup(value: AttributeValue): AttributeNode {
  return Attr("onkeyup", value);
}

/**
 * Fires when a resource finishes loading.
 *
 * @example Usage
 * ```ts
 * import { Onload } from "@varavel/nodx";
 *
 * const node = Onload("value");
 * // Renders as: onload="value"
 * ```
 */
export function Onload(value: AttributeValue): AttributeNode {
  return Attr("onload", value);
}

/**
 * Fires when the first frame of the media is loaded.
 *
 * @example Usage
 * ```ts
 * import { Onloadeddata } from "@varavel/nodx";
 *
 * const node = Onloadeddata("value");
 * // Renders as: onloadeddata="value"
 * ```
 */
export function Onloadeddata(value: AttributeValue): AttributeNode {
  return Attr("onloadeddata", value);
}

/**
 * Fires when the metadata of the media is loaded.
 *
 * @example Usage
 * ```ts
 * import { Onloadedmetadata } from "@varavel/nodx";
 *
 * const node = Onloadedmetadata("value");
 * // Renders as: onloadedmetadata="value"
 * ```
 */
export function Onloadedmetadata(value: AttributeValue): AttributeNode {
  return Attr("onloadedmetadata", value);
}

/**
 * Fires when the loading of a resource starts.
 *
 * @example Usage
 * ```ts
 * import { Onloadstart } from "@varavel/nodx";
 *
 * const node = Onloadstart("value");
 * // Renders as: onloadstart="value"
 * ```
 */
export function Onloadstart(value: AttributeValue): AttributeNode {
  return Attr("onloadstart", value);
}

/**
 * Fires when pointer capture is released.
 *
 * @example Usage
 * ```ts
 * import { Onlostpointercapture } from "@varavel/nodx";
 *
 * const node = Onlostpointercapture("value");
 * // Renders as: onlostpointercapture="value"
 * ```
 */
export function Onlostpointercapture(value: AttributeValue): AttributeNode {
  return Attr("onlostpointercapture", value);
}

/**
 * Fires when a mouse button is pressed down.
 *
 * @example Usage
 * ```ts
 * import { Onmousedown } from "@varavel/nodx";
 *
 * const node = Onmousedown("value");
 * // Renders as: onmousedown="value"
 * ```
 */
export function Onmousedown(value: AttributeValue): AttributeNode {
  return Attr("onmousedown", value);
}

/**
 * Fires when the pointer enters an element.
 *
 * @example Usage
 * ```ts
 * import { Onmouseenter } from "@varavel/nodx";
 *
 * const node = Onmouseenter("value");
 * // Renders as: onmouseenter="value"
 * ```
 */
export function Onmouseenter(value: AttributeValue): AttributeNode {
  return Attr("onmouseenter", value);
}

/**
 * Fires when the pointer leaves an element.
 *
 * @example Usage
 * ```ts
 * import { Onmouseleave } from "@varavel/nodx";
 *
 * const node = Onmouseleave("value");
 * // Renders as: onmouseleave="value"
 * ```
 */
export function Onmouseleave(value: AttributeValue): AttributeNode {
  return Attr("onmouseleave", value);
}

/**
 * Fires when the pointer moves over an element.
 *
 * @example Usage
 * ```ts
 * import { Onmousemove } from "@varavel/nodx";
 *
 * const node = Onmousemove("value");
 * // Renders as: onmousemove="value"
 * ```
 */
export function Onmousemove(value: AttributeValue): AttributeNode {
  return Attr("onmousemove", value);
}

/**
 * Fires when the pointer leaves an element or one of its children.
 *
 * @example Usage
 * ```ts
 * import { Onmouseout } from "@varavel/nodx";
 *
 * const node = Onmouseout("value");
 * // Renders as: onmouseout="value"
 * ```
 */
export function Onmouseout(value: AttributeValue): AttributeNode {
  return Attr("onmouseout", value);
}

/**
 * Fires when the pointer moves onto an element.
 *
 * @example Usage
 * ```ts
 * import { Onmouseover } from "@varavel/nodx";
 *
 * const node = Onmouseover("value");
 * // Renders as: onmouseover="value"
 * ```
 */
export function Onmouseover(value: AttributeValue): AttributeNode {
  return Attr("onmouseover", value);
}

/**
 * Fires when a mouse button is released.
 *
 * @example Usage
 * ```ts
 * import { Onmouseup } from "@varavel/nodx";
 *
 * const node = Onmouseup("value");
 * // Renders as: onmouseup="value"
 * ```
 */
export function Onmouseup(value: AttributeValue): AttributeNode {
  return Attr("onmouseup", value);
}

/**
 * Fires when the user pastes content.
 *
 * @example Usage
 * ```ts
 * import { Onpaste } from "@varavel/nodx";
 *
 * const node = Onpaste("value");
 * // Renders as: onpaste="value"
 * ```
 */
export function Onpaste(value: AttributeValue): AttributeNode {
  return Attr("onpaste", value);
}

/**
 * Fires when the media is paused.
 *
 * @example Usage
 * ```ts
 * import { Onpause } from "@varavel/nodx";
 *
 * const node = Onpause("value");
 * // Renders as: onpause="value"
 * ```
 */
export function Onpause(value: AttributeValue): AttributeNode {
  return Attr("onpause", value);
}

/**
 * Fires when the media starts playing.
 *
 * @example Usage
 * ```ts
 * import { Onplay } from "@varavel/nodx";
 *
 * const node = Onplay("value");
 * // Renders as: onplay="value"
 * ```
 */
export function Onplay(value: AttributeValue): AttributeNode {
  return Attr("onplay", value);
}

/**
 * Fires when the media is playing.
 *
 * @example Usage
 * ```ts
 * import { Onplaying } from "@varavel/nodx";
 *
 * const node = Onplaying("value");
 * // Renders as: onplaying="value"
 * ```
 */
export function Onplaying(value: AttributeValue): AttributeNode {
  return Attr("onplaying", value);
}

/**
 * Fires when a pointer event is cancelled.
 *
 * @example Usage
 * ```ts
 * import { Onpointercancel } from "@varavel/nodx";
 *
 * const node = Onpointercancel("value");
 * // Renders as: onpointercancel="value"
 * ```
 */
export function Onpointercancel(value: AttributeValue): AttributeNode {
  return Attr("onpointercancel", value);
}

/**
 * Fires when a pointer is pressed down.
 *
 * @example Usage
 * ```ts
 * import { Onpointerdown } from "@varavel/nodx";
 *
 * const node = Onpointerdown("value");
 * // Renders as: onpointerdown="value"
 * ```
 */
export function Onpointerdown(value: AttributeValue): AttributeNode {
  return Attr("onpointerdown", value);
}

/**
 * Fires when the pointer enters the hit test area.
 *
 * @example Usage
 * ```ts
 * import { Onpointerenter } from "@varavel/nodx";
 *
 * const node = Onpointerenter("value");
 * // Renders as: onpointerenter="value"
 * ```
 */
export function Onpointerenter(value: AttributeValue): AttributeNode {
  return Attr("onpointerenter", value);
}

/**
 * Fires when the pointer leaves the hit test area.
 *
 * @example Usage
 * ```ts
 * import { Onpointerleave } from "@varavel/nodx";
 *
 * const node = Onpointerleave("value");
 * // Renders as: onpointerleave="value"
 * ```
 */
export function Onpointerleave(value: AttributeValue): AttributeNode {
  return Attr("onpointerleave", value);
}

/**
 * Fires when the pointer moves.
 *
 * @example Usage
 * ```ts
 * import { Onpointermove } from "@varavel/nodx";
 *
 * const node = Onpointermove("value");
 * // Renders as: onpointermove="value"
 * ```
 */
export function Onpointermove(value: AttributeValue): AttributeNode {
  return Attr("onpointermove", value);
}

/**
 * Fires when the pointer leaves the hit test boundaries.
 *
 * @example Usage
 * ```ts
 * import { Onpointerout } from "@varavel/nodx";
 *
 * const node = Onpointerout("value");
 * // Renders as: onpointerout="value"
 * ```
 */
export function Onpointerout(value: AttributeValue): AttributeNode {
  return Attr("onpointerout", value);
}

/**
 * Fires when the pointer moves onto the hit test area.
 *
 * @example Usage
 * ```ts
 * import { Onpointerover } from "@varavel/nodx";
 *
 * const node = Onpointerover("value");
 * // Renders as: onpointerover="value"
 * ```
 */
export function Onpointerover(value: AttributeValue): AttributeNode {
  return Attr("onpointerover", value);
}

/**
 * Fires when the pointer is released.
 *
 * @example Usage
 * ```ts
 * import { Onpointerup } from "@varavel/nodx";
 *
 * const node = Onpointerup("value");
 * // Renders as: onpointerup="value"
 * ```
 */
export function Onpointerup(value: AttributeValue): AttributeNode {
  return Attr("onpointerup", value);
}

/**
 * Fires while the media is loading.
 *
 * @example Usage
 * ```ts
 * import { Onprogress } from "@varavel/nodx";
 *
 * const node = Onprogress("value");
 * // Renders as: onprogress="value"
 * ```
 */
export function Onprogress(value: AttributeValue): AttributeNode {
  return Attr("onprogress", value);
}

/**
 * Fires when the playback rate changes.
 *
 * @example Usage
 * ```ts
 * import { Onratechange } from "@varavel/nodx";
 *
 * const node = Onratechange("value");
 * // Renders as: onratechange="value"
 * ```
 */
export function Onratechange(value: AttributeValue): AttributeNode {
  return Attr("onratechange", value);
}

/**
 * Fires when a form is reset.
 *
 * @example Usage
 * ```ts
 * import { Onreset } from "@varavel/nodx";
 *
 * const node = Onreset("value");
 * // Renders as: onreset="value"
 * ```
 */
export function Onreset(value: AttributeValue): AttributeNode {
  return Attr("onreset", value);
}

/**
 * Fires when the document view is resized.
 *
 * @example Usage
 * ```ts
 * import { Onresize } from "@varavel/nodx";
 *
 * const node = Onresize("value");
 * // Renders as: onresize="value"
 * ```
 */
export function Onresize(value: AttributeValue): AttributeNode {
  return Attr("onresize", value);
}

/**
 * Fires when the document or an element is scrolled.
 *
 * @example Usage
 * ```ts
 * import { Onscroll } from "@varavel/nodx";
 *
 * const node = Onscroll("value");
 * // Renders as: onscroll="value"
 * ```
 */
export function Onscroll(value: AttributeValue): AttributeNode {
  return Attr("onscroll", value);
}

/**
 * Fires when scrolling ends.
 *
 * @example Usage
 * ```ts
 * import { Onscrollend } from "@varavel/nodx";
 *
 * const node = Onscrollend("value");
 * // Renders as: onscrollend="value"
 * ```
 */
export function Onscrollend(value: AttributeValue): AttributeNode {
  return Attr("onscrollend", value);
}

/**
 * Fires when a Content Security Policy is violated.
 *
 * @example Usage
 * ```ts
 * import { Onsecuritypolicyviolation } from "@varavel/nodx";
 *
 * const node = Onsecuritypolicyviolation("value");
 * // Renders as: onsecuritypolicyviolation="value"
 * ```
 */
export function Onsecuritypolicyviolation(value: AttributeValue): AttributeNode {
  return Attr("onsecuritypolicyviolation", value);
}

/**
 * Fires when a seek operation finishes.
 *
 * @example Usage
 * ```ts
 * import { Onseeked } from "@varavel/nodx";
 *
 * const node = Onseeked("value");
 * // Renders as: onseeked="value"
 * ```
 */
export function Onseeked(value: AttributeValue): AttributeNode {
  return Attr("onseeked", value);
}

/**
 * Fires when a seek operation starts.
 *
 * @example Usage
 * ```ts
 * import { Onseeking } from "@varavel/nodx";
 *
 * const node = Onseeking("value");
 * // Renders as: onseeking="value"
 * ```
 */
export function Onseeking(value: AttributeValue): AttributeNode {
  return Attr("onseeking", value);
}

/**
 * Fires when text is selected.
 *
 * @example Usage
 * ```ts
 * import { Onselect } from "@varavel/nodx";
 *
 * const node = Onselect("value");
 * // Renders as: onselect="value"
 * ```
 */
export function Onselect(value: AttributeValue): AttributeNode {
  return Attr("onselect", value);
}

/**
 * Fires when the assigned nodes of a slot change.
 *
 * @example Usage
 * ```ts
 * import { Onslotchange } from "@varavel/nodx";
 *
 * const node = Onslotchange("value");
 * // Renders as: onslotchange="value"
 * ```
 */
export function Onslotchange(value: AttributeValue): AttributeNode {
  return Attr("onslotchange", value);
}

/**
 * Fires when the media is stalled.
 *
 * @example Usage
 * ```ts
 * import { Onstalled } from "@varavel/nodx";
 *
 * const node = Onstalled("value");
 * // Renders as: onstalled="value"
 * ```
 */
export function Onstalled(value: AttributeValue): AttributeNode {
  return Attr("onstalled", value);
}

/**
 * Fires when a form is submitted.
 *
 * @example Usage
 * ```ts
 * import { Onsubmit } from "@varavel/nodx";
 *
 * const node = Onsubmit("value");
 * // Renders as: onsubmit="value"
 * ```
 */
export function Onsubmit(value: AttributeValue): AttributeNode {
  return Attr("onsubmit", value);
}

/**
 * Fires when the media is suspended.
 *
 * @example Usage
 * ```ts
 * import { Onsuspend } from "@varavel/nodx";
 *
 * const node = Onsuspend("value");
 * // Renders as: onsuspend="value"
 * ```
 */
export function Onsuspend(value: AttributeValue): AttributeNode {
  return Attr("onsuspend", value);
}

/**
 * Fires when the playback position changes.
 *
 * @example Usage
 * ```ts
 * import { Ontimeupdate } from "@varavel/nodx";
 *
 * const node = Ontimeupdate("value");
 * // Renders as: ontimeupdate="value"
 * ```
 */
export function Ontimeupdate(value: AttributeValue): AttributeNode {
  return Attr("ontimeupdate", value);
}

/**
 * Fires when a details or popover element toggles.
 *
 * @example Usage
 * ```ts
 * import { Ontoggle } from "@varavel/nodx";
 *
 * const node = Ontoggle("value");
 * // Renders as: ontoggle="value"
 * ```
 */
export function Ontoggle(value: AttributeValue): AttributeNode {
  return Attr("ontoggle", value);
}

/**
 * Fires when a CSS transition is cancelled.
 *
 * @example Usage
 * ```ts
 * import { Ontransitioncancel } from "@varavel/nodx";
 *
 * const node = Ontransitioncancel("value");
 * // Renders as: ontransitioncancel="value"
 * ```
 */
export function Ontransitioncancel(value: AttributeValue): AttributeNode {
  return Attr("ontransitioncancel", value);
}

/**
 * Fires when a CSS transition finishes.
 *
 * @example Usage
 * ```ts
 * import { Ontransitionend } from "@varavel/nodx";
 *
 * const node = Ontransitionend("value");
 * // Renders as: ontransitionend="value"
 * ```
 */
export function Ontransitionend(value: AttributeValue): AttributeNode {
  return Attr("ontransitionend", value);
}

/**
 * Fires when a CSS transition starts running.
 *
 * @example Usage
 * ```ts
 * import { Ontransitionrun } from "@varavel/nodx";
 *
 * const node = Ontransitionrun("value");
 * // Renders as: ontransitionrun="value"
 * ```
 */
export function Ontransitionrun(value: AttributeValue): AttributeNode {
  return Attr("ontransitionrun", value);
}

/**
 * Fires when a CSS transition starts.
 *
 * @example Usage
 * ```ts
 * import { Ontransitionstart } from "@varavel/nodx";
 *
 * const node = Ontransitionstart("value");
 * // Renders as: ontransitionstart="value"
 * ```
 */
export function Ontransitionstart(value: AttributeValue): AttributeNode {
  return Attr("ontransitionstart", value);
}

/**
 * Fires when the media volume changes.
 *
 * @example Usage
 * ```ts
 * import { Onvolumechange } from "@varavel/nodx";
 *
 * const node = Onvolumechange("value");
 * // Renders as: onvolumechange="value"
 * ```
 */
export function Onvolumechange(value: AttributeValue): AttributeNode {
  return Attr("onvolumechange", value);
}

/**
 * Fires when the media is waiting for data.
 *
 * @example Usage
 * ```ts
 * import { Onwaiting } from "@varavel/nodx";
 *
 * const node = Onwaiting("value");
 * // Renders as: onwaiting="value"
 * ```
 */
export function Onwaiting(value: AttributeValue): AttributeNode {
  return Attr("onwaiting", value);
}

/**
 * Fires when the mouse wheel is rotated.
 *
 * @example Usage
 * ```ts
 * import { Onwheel } from "@varavel/nodx";
 *
 * const node = Onwheel("value");
 * // Renders as: onwheel="value"
 * ```
 */
export function Onwheel(value: AttributeValue): AttributeNode {
  return Attr("onwheel", value);
}

/**
 * Specifies that the details should be visible to the user.
 *
 * @example Usage
 * ```ts
 * import { Open, Input } from "@varavel/nodx";
 *
 * const node = Input(Open(true));
 * // Renders as: <input open>
 *
 * // When the value is `false`, the attribute is omitted entirely:
 * const omitted = Input(Open(false));
 * // Renders as: <input>
 * ```
 */
export function Open(value: boolean): Node {
  return AttrBool("open", value);
}

/**
 * Specifies the optimal value of the gauge.
 *
 * @example Usage
 * ```ts
 * import { Optimum } from "@varavel/nodx";
 *
 * const node = Optimum("value");
 * // Renders as: optimum="value"
 * ```
 */
export function Optimum(value: AttributeValue): AttributeNode {
  return Attr("optimum", value);
}

/**
 * Identifies the element as a part of a shadow DOM.
 *
 * @example Usage
 * ```ts
 * import { Part } from "@varavel/nodx";
 *
 * const node = Part("value-a value-b", "value-c");
 * // Renders as: part="value-a value-b value-c"
 * ```
 */
export function Part(
  ...values: readonly AttributeValue[]
): AttributeNode {
  return AttrList("part", ...values);
}

/**
 * Specifies a regular expression that the input element's value is checked against.
 *
 * @example Usage
 * ```ts
 * import { Pattern } from "@varavel/nodx";
 *
 * const node = Pattern("value");
 * // Renders as: pattern="value"
 * ```
 */
export function Pattern(value: AttributeValue): AttributeNode {
  return Attr("pattern", value);
}

/**
 * Specifies a space-separated list of URLs to be notified if a user follows the hyperlink.
 *
 * @example Usage
 * ```ts
 * import { Ping } from "@varavel/nodx";
 *
 * const node = Ping("value-a value-b", "value-c");
 * // Renders as: ping="value-a value-b value-c"
 * ```
 */
export function Ping(
  ...values: readonly AttributeValue[]
): AttributeNode {
  return AttrList("ping", ...values);
}

/**
 * Specifies a short hint that describes the expected value of an input field.
 *
 * @example Usage
 * ```ts
 * import { Placeholder } from "@varavel/nodx";
 *
 * const node = Placeholder("value");
 * // Renders as: placeholder="value"
 * ```
 */
export function Placeholder(value: AttributeValue): AttributeNode {
  return Attr("placeholder", value);
}

/**
 * Indicates that the video should play inline on mobile devices.
 *
 * @example Usage
 * ```ts
 * import { Playsinline, Input } from "@varavel/nodx";
 *
 * const node = Input(Playsinline(true));
 * // Renders as: <input playsinline>
 *
 * // When the value is `false`, the attribute is omitted entirely:
 * const omitted = Input(Playsinline(false));
 * // Renders as: <input>
 * ```
 */
export function Playsinline(value: boolean): Node {
  return AttrBool("playsinline", value);
}

/**
 * Marks an element as a popover.
 *
 * @example Usage
 * ```ts
 * import { Popover } from "@varavel/nodx";
 *
 * const node = Popover("value");
 * // Renders as: popover="value"
 * ```
 */
export function Popover(value: AttributeValue): AttributeNode {
  return Attr("popover", value);
}

/**
 * Specifies the id of the popover element to toggle.
 *
 * @example Usage
 * ```ts
 * import { Popovertarget } from "@varavel/nodx";
 *
 * const node = Popovertarget("value");
 * // Renders as: popovertarget="value"
 * ```
 */
export function Popovertarget(value: AttributeValue): AttributeNode {
  return Attr("popovertarget", value);
}

/**
 * Specifies the action to perform on the popover target.
 *
 * @example Usage
 * ```ts
 * import { Popovertargetaction } from "@varavel/nodx";
 *
 * const node = Popovertargetaction("value");
 * // Renders as: popovertargetaction="value"
 * ```
 */
export function Popovertargetaction(value: AttributeValue): AttributeNode {
  return Attr("popovertargetaction", value);
}

/**
 * Specifies an image to be shown while the video is downloading or until the user hits the play button.
 *
 * @example Usage
 * ```ts
 * import { Poster } from "@varavel/nodx";
 *
 * const node = Poster("value");
 * // Renders as: poster="value"
 * ```
 */
export function Poster(value: AttributeValue): AttributeNode {
  return Attr("poster", value);
}

/**
 * Specifies if and how the author thinks the audio/video should be loaded when the page loads.
 *
 * @example Usage
 * ```ts
 * import { Preload } from "@varavel/nodx";
 *
 * const node = Preload("value");
 * // Renders as: preload="value"
 * ```
 */
export function Preload(value: AttributeValue): AttributeNode {
  return Attr("preload", value);
}

/**
 * Specifies the group of radio buttons a command belongs to (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Radiogroup } from "@varavel/nodx";
 *
 * const node = Radiogroup("value");
 * // Renders as: radiogroup="value"
 * ```
 */
export function Radiogroup(value: AttributeValue): AttributeNode {
  return Attr("radiogroup", value);
}

/**
 * Specifies that the input field is read-only.
 *
 * @example Usage
 * ```ts
 * import { Readonly, Input } from "@varavel/nodx";
 *
 * const node = Input(Readonly(true));
 * // Renders as: <input readonly>
 *
 * // When the value is `false`, the attribute is omitted entirely:
 * const omitted = Input(Readonly(false));
 * // Renders as: <input>
 * ```
 */
export function Readonly(value: boolean): Node {
  return AttrBool("readonly", value);
}

/**
 * Specifies which referrer information to send when fetching a resource.
 *
 * @example Usage
 * ```ts
 * import { Referrerpolicy } from "@varavel/nodx";
 *
 * const node = Referrerpolicy("value");
 * // Renders as: referrerpolicy="value"
 * ```
 */
export function Referrerpolicy(value: AttributeValue): AttributeNode {
  return Attr("referrerpolicy", value);
}

/**
 * Specifies the relationship between the current document and the linked document.
 *
 * @example Usage
 * ```ts
 * import { Rel } from "@varavel/nodx";
 *
 * const node = Rel("value-a value-b", "value-c");
 * // Renders as: rel="value-a value-b value-c"
 * ```
 */
export function Rel(
  ...values: readonly AttributeValue[]
): AttributeNode {
  return AttrList("rel", ...values);
}

/**
 * Specifies that the input field must be filled out before submitting the form.
 *
 * @example Usage
 * ```ts
 * import { Required, Input } from "@varavel/nodx";
 *
 * const node = Input(Required(true));
 * // Renders as: <input required>
 *
 * // When the value is `false`, the attribute is omitted entirely:
 * const omitted = Input(Required(false));
 * // Renders as: <input>
 * ```
 */
export function Required(value: boolean): Node {
  return AttrBool("required", value);
}

/**
 * Specifies the maximum number of results to show in a search input (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Results } from "@varavel/nodx";
 *
 * const node = Results("value");
 * // Renders as: results="value"
 * ```
 */
export function Results(value: AttributeValue): AttributeNode {
  return Attr("results", value);
}

/**
 * Specifies that the list order should be descending (9,8,7...).
 *
 * @example Usage
 * ```ts
 * import { Reversed, Input } from "@varavel/nodx";
 *
 * const node = Input(Reversed(true));
 * // Renders as: <input reversed>
 *
 * // When the value is `false`, the attribute is omitted entirely:
 * const omitted = Input(Reversed(false));
 * // Renders as: <input>
 * ```
 */
export function Reversed(value: boolean): Node {
  return AttrBool("reversed", value);
}

/**
 * Defines the role of an element for accessibility purposes.
 *
 * @example Usage
 * ```ts
 * import { Role } from "@varavel/nodx";
 *
 * const node = Role("value");
 * // Renders as: role="value"
 * ```
 */
export function Role(value: AttributeValue): AttributeNode {
  return Attr("role", value);
}

/**
 * Specifies the visible number of lines in a text area.
 *
 * @example Usage
 * ```ts
 * import { Rows } from "@varavel/nodx";
 *
 * const node = Rows("value");
 * // Renders as: rows="value"
 * ```
 */
export function Rows(value: AttributeValue): AttributeNode {
  return Attr("rows", value);
}

/**
 * Specifies the number of rows a cell should span.
 *
 * @example Usage
 * ```ts
 * import { Rowspan } from "@varavel/nodx";
 *
 * const node = Rowspan("value");
 * // Renders as: rowspan="value"
 * ```
 */
export function Rowspan(value: AttributeValue): AttributeNode {
  return Attr("rowspan", value);
}

/**
 * Specifies which rules to display between table cells (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Rules } from "@varavel/nodx";
 *
 * const node = Rules("value");
 * // Renders as: rules="value"
 * ```
 */
export function Rules(value: AttributeValue): AttributeNode {
  return Attr("rules", value);
}

/**
 * Enables an extra set of restrictions for the content in an iframe.
 *
 * @example Usage
 * ```ts
 * import { Sandbox } from "@varavel/nodx";
 *
 * const node = Sandbox("value-a value-b", "value-c");
 * // Renders as: sandbox="value-a value-b value-c"
 * ```
 */
export function Sandbox(
  ...values: readonly AttributeValue[]
): AttributeNode {
  return AttrList("sandbox", ...values);
}

/**
 * Specifies whether a header cell is a header for a column, row, or group of columns or rows.
 *
 * @example Usage
 * ```ts
 * import { Scope } from "@varavel/nodx";
 *
 * const node = Scope("value");
 * // Renders as: scope="value"
 * ```
 */
export function Scope(value: AttributeValue): AttributeNode {
  return Attr("scope", value);
}

/**
 * Specifies that the styles only apply to this element's parent and child elements.
 *
 * @example Usage
 * ```ts
 * import { Scoped } from "@varavel/nodx";
 *
 * const node = Scoped("value");
 * // Renders as: scoped="value"
 * ```
 */
export function Scoped(value: AttributeValue): AttributeNode {
  return Attr("scoped", value);
}

/**
 * Specifies whether to display scrollbars in an iframe (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Scrolling } from "@varavel/nodx";
 *
 * const node = Scrolling("value");
 * // Renders as: scrolling="value"
 * ```
 */
export function Scrolling(value: AttributeValue): AttributeNode {
  return Attr("scrolling", value);
}

/**
 * Specifies that an option should be pre-selected when the page loads.
 *
 * @example Usage
 * ```ts
 * import { Selected, Input } from "@varavel/nodx";
 *
 * const node = Input(Selected(true));
 * // Renders as: <input selected>
 *
 * // When the value is `false`, the attribute is omitted entirely:
 * const omitted = Input(Selected(false));
 * // Renders as: <input>
 * ```
 */
export function Selected(value: boolean): Node {
  return AttrBool("selected", value);
}

/**
 * Specifies the shape of an area in an image-map.
 *
 * @example Usage
 * ```ts
 * import { Shape } from "@varavel/nodx";
 *
 * const node = Shape("value");
 * // Renders as: shape="value"
 * ```
 */
export function Shape(value: AttributeValue): AttributeNode {
  return Attr("shape", value);
}

/**
 * Specifies the width, in characters, of an input field.
 *
 * @example Usage
 * ```ts
 * import { Size } from "@varavel/nodx";
 *
 * const node = Size("value");
 * // Renders as: size="value"
 * ```
 */
export function Size(value: AttributeValue): AttributeNode {
  return Attr("size", value);
}

/**
 * Specifies the sizes of icons for visual media.
 *
 * @example Usage
 * ```ts
 * import { Sizes } from "@varavel/nodx";
 *
 * const node = Sizes("value");
 * // Renders as: sizes="value"
 * ```
 */
export function Sizes(value: AttributeValue): AttributeNode {
  return Attr("sizes", value);
}

/**
 * Assigns a slot in a shadow DOM shadow tree.
 *
 * @example Usage
 * ```ts
 * import { SlotAttr } from "@varavel/nodx";
 *
 * const node = SlotAttr("value");
 * // Renders as: slot="value"
 * ```
 */
export function SlotAttr(value: AttributeValue): AttributeNode {
  return Attr("slot", value);
}

/**
 * Defines the number of columns to span for a col or colgroup element.
 *
 * @example Usage
 * ```ts
 * import { SpanAttr } from "@varavel/nodx";
 *
 * const node = SpanAttr("value");
 * // Renders as: span="value"
 * ```
 */
export function SpanAttr(value: AttributeValue): AttributeNode {
  return Attr("span", value);
}

/**
 * Specifies whether the element is to have its spelling and grammar checked or not.
 *
 * @example Usage
 * ```ts
 * import { Spellcheck } from "@varavel/nodx";
 *
 * const node = Spellcheck("value");
 * // Renders as: spellcheck="value"
 * ```
 */
export function Spellcheck(value: AttributeValue): AttributeNode {
  return Attr("spellcheck", value);
}

/**
 * Specifies the URL of the media file.
 *
 * @example Usage
 * ```ts
 * import { Src } from "@varavel/nodx";
 *
 * const node = Src("value");
 * // Renders as: src="value"
 * ```
 */
export function Src(value: AttributeValue): AttributeNode {
  return Attr("src", value);
}

/**
 * Specifies the HTML content of the page to show in the iframe.
 *
 * @example Usage
 * ```ts
 * import { Srcdoc } from "@varavel/nodx";
 *
 * const node = Srcdoc("value");
 * // Renders as: srcdoc="value"
 * ```
 */
export function Srcdoc(value: AttributeValue): AttributeNode {
  return Attr("srcdoc", value);
}

/**
 * Specifies the language of the track text data (required if kind='subtitles').
 *
 * @example Usage
 * ```ts
 * import { Srclang } from "@varavel/nodx";
 *
 * const node = Srclang("value");
 * // Renders as: srclang="value"
 * ```
 */
export function Srclang(value: AttributeValue): AttributeNode {
  return Attr("srclang", value);
}

/**
 * Specifies the URL of the image to use in different situations.
 *
 * @example Usage
 * ```ts
 * import { Srcset } from "@varavel/nodx";
 *
 * const node = Srcset("value");
 * // Renders as: srcset="value"
 * ```
 */
export function Srcset(value: AttributeValue): AttributeNode {
  return Attr("srcset", value);
}

/**
 * Specifies a message to show while an object loads (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Standby } from "@varavel/nodx";
 *
 * const node = Standby("value");
 * // Renders as: standby="value"
 * ```
 */
export function Standby(value: AttributeValue): AttributeNode {
  return Attr("standby", value);
}

/**
 * Specifies the start value of an ordered list.
 *
 * @example Usage
 * ```ts
 * import { Start } from "@varavel/nodx";
 *
 * const node = Start("value");
 * // Renders as: start="value"
 * ```
 */
export function Start(value: AttributeValue): AttributeNode {
  return Attr("start", value);
}

/**
 * Specifies the legal number intervals for an input field.
 *
 * @example Usage
 * ```ts
 * import { Step } from "@varavel/nodx";
 *
 * const node = Step("value");
 * // Renders as: step="value"
 * ```
 */
export function Step(value: AttributeValue): AttributeNode {
  return Attr("step", value);
}

/**
 * Specifies an inline CSS style for an element.
 *
 * @example Usage
 * ```ts
 * import { StyleAttr } from "@varavel/nodx";
 *
 * const node = StyleAttr("value");
 * // Renders as: style="value"
 * ```
 */
export function StyleAttr(value: AttributeValue): AttributeNode {
  return Attr("style", value);
}

/**
 * Specifies a summary of the content of a table (deprecated in HTML5).
 *
 * @example Usage
 * ```ts
 * import { SummaryAttr } from "@varavel/nodx";
 *
 * const node = SummaryAttr("value");
 * // Renders as: summary="value"
 * ```
 */
export function SummaryAttr(value: AttributeValue): AttributeNode {
  return Attr("summary", value);
}

/**
 * Specifies the tabbing order of an element.
 *
 * @example Usage
 * ```ts
 * import { Tabindex } from "@varavel/nodx";
 *
 * const node = Tabindex("value");
 * // Renders as: tabindex="value"
 * ```
 */
export function Tabindex(value: AttributeValue): AttributeNode {
  return Attr("tabindex", value);
}

/**
 * Specifies where to open the linked document.
 *
 * @example Usage
 * ```ts
 * import { Target } from "@varavel/nodx";
 *
 * const node = Target("value");
 * // Renders as: target="value"
 * ```
 */
export function Target(value: AttributeValue): AttributeNode {
  return Attr("target", value);
}

/**
 * Specifies extra information about an element.
 *
 * @example Usage
 * ```ts
 * import { TitleAttr } from "@varavel/nodx";
 *
 * const node = TitleAttr("value");
 * // Renders as: title="value"
 * ```
 */
export function TitleAttr(value: AttributeValue): AttributeNode {
  return Attr("title", value);
}

/**
 * Specifies whether the content of an element should be translated or not.
 *
 * @example Usage
 * ```ts
 * import { Translate } from "@varavel/nodx";
 *
 * const node = Translate("value");
 * // Renders as: translate="value"
 * ```
 */
export function Translate(value: AttributeValue): AttributeNode {
  return Attr("translate", value);
}

/**
 * Specifies the type of element.
 *
 * @example Usage
 * ```ts
 * import { Type } from "@varavel/nodx";
 *
 * const node = Type("value");
 * // Renders as: type="value"
 * ```
 */
export function Type(value: AttributeValue): AttributeNode {
  return Attr("type", value);
}

/**
 * Specifies an image as a client-side image-map.
 *
 * @example Usage
 * ```ts
 * import { Usemap } from "@varavel/nodx";
 *
 * const node = Usemap("value");
 * // Renders as: usemap="value"
 * ```
 */
export function Usemap(value: AttributeValue): AttributeNode {
  return Attr("usemap", value);
}

/**
 * Specifies the vertical alignment of the content (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Valign } from "@varavel/nodx";
 *
 * const node = Valign("value");
 * // Renders as: valign="value"
 * ```
 */
export function Valign(value: AttributeValue): AttributeNode {
  return Attr("valign", value);
}

/**
 * Specifies the value of the element.
 *
 * @example Usage
 * ```ts
 * import { Value } from "@varavel/nodx";
 *
 * const node = Value("value");
 * // Renders as: value="value"
 * ```
 */
export function Value(value: AttributeValue): AttributeNode {
  return Attr("value", value);
}

/**
 * Specifies the type of the value of a param element (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Valuetype } from "@varavel/nodx";
 *
 * const node = Valuetype("value");
 * // Renders as: valuetype="value"
 * ```
 */
export function Valuetype(value: AttributeValue): AttributeNode {
  return Attr("valuetype", value);
}

/**
 * Specifies the vertical space around an element (deprecated).
 *
 * @example Usage
 * ```ts
 * import { Vspace } from "@varavel/nodx";
 *
 * const node = Vspace("value");
 * // Renders as: vspace="value"
 * ```
 */
export function Vspace(value: AttributeValue): AttributeNode {
  return Attr("vspace", value);
}

/**
 * Specifies the width of the element.
 *
 * @example Usage
 * ```ts
 * import { Width } from "@varavel/nodx";
 *
 * const node = Width("value");
 * // Renders as: width="value"
 * ```
 */
export function Width(value: AttributeValue): AttributeNode {
  return Attr("width", value);
}

/**
 * Specifies how the text in a text area is to be wrapped when submitted in a form.
 *
 * @example Usage
 * ```ts
 * import { Wrap } from "@varavel/nodx";
 *
 * const node = Wrap("value");
 * // Renders as: wrap="value"
 * ```
 */
export function Wrap(value: AttributeValue): AttributeNode {
  return Attr("wrap", value);
}
